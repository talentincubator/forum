{
  // Server authentication info
  "servers": [
    {
      "host": "188.166.52.166",
      "username": "root",
      "password": "talent2015"
      // or pem file (ssh based authentication)
      //"pem": "~/.ssh/id_rsa"
    }
  ],

  // Install MongoDB in the server, does not destroy local MongoDB on future setup
  "setupMongo": true,

  // WARNING: Node.js is required! Only skip if you already have Node.js installed on server.
  "setupNode": true,

  // WARNING: If nodeVersion omitted will setup 0.10.33 by default. Do not use v, only version number.
  "nodeVersion": "0.10.33",

  // Install PhantomJS in the server
  "setupPhantom": true,

  // Application name (No spaces)
  "appName": "talentincubator",

  // Location of app (local directory)
  "app": ".",

  // Configure environment
  "env": {
    "PORT":3001,
    "ROOT_URL": "http://localhost",
    "MONGO_URL": "mongodb://127.0.0.1:27017/talentincubator",
    "MONGO_OPLOG_URL":"mongodb://oplogger:pwd@127.0.0.1:27017/local?authSource=admin",
    "DDP_DEFAULT_CONNECTION_URL":"http://188.166.52.166:3001",
    "DISABLE_WEBSOCKETS": "1"
  },

  // Meteor Up checks if the app comes online just after the deployment
  // before mup checks that, it will wait for no. of seconds configured below
  "deployCheckWaitTime": 20
}
